# Release ownership

- Use `obewi/SkiNavIndexes` for repository, raw-content, and release URLs.
- Do not reintroduce `OrbitalExplorer` in generated metadata, app update endpoints, workflow examples, or README release instructions.
