"""Index builder for SkiNavIndexes."""

from typing import Any, Dict


def build_index(data: Dict[str, Any]) -> Dict[str, Any]:
    """Build a simple index structure from input data.

    This is a placeholder implementation. It currently returns an
    empty index structure to establish the API surface for future
    implementation.
    """

    # Placeholder: return the input as-is wrapped in an index container
    return {"index": data}
